metadata:
  summary: "Fedora COSMIC Atomic"

variables:
  variant: "cosmic-atomic"

ref: fedora/43/${basearch}/cosmic-atomic

include:
  - common.yaml
  - packages/cosmic-atomic.yaml

packages:
  - fedora-release-cosmic-atomic
  # Portals for Flatpak
  - xdg-desktop-portal-gtk
