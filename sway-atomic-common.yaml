metadata:
  summary: "Fedora Sway Atomic"

variables:
  variant: "sway-atomic"

ref: fedora/43/${basearch}/sericea

include:
  - common.yaml
  - packages/sway-atomic.yaml

packages:
  - fedora-release-sway-atomic
  # explicitly list some defaults
  - firefox

exclude-packages:
  # nothing in the tree would use these
  - tracker
  - tracker-miners
  - localsearch
  - tinysparql
